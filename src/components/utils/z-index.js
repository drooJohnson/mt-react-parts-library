let base = 0;
let low  = 100;
let mid  = 200;
let high = 300;
let top  = 400;

let zIndex = {
  base:      base, // 0 * 100
    scrimLow:  low,  // 1 * 100
  low:       low,  // 1 * 100
    middle:    mid,  // 2 * 100
  mid:       mid,  // 2 * 100
    scrimHigh: high, // 3 * 100
  high:      high, // 3 * 100
  top:       top,  // 4 * 100
}

export default zIndex;
